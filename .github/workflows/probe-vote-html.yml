name: 投票記録HTML構造プローブ（実現可能性調査）

on:
  workflow_dispatch:

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: プローブ実行
        run: python3 scripts/probe_vote_html.py
